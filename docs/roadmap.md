# Overlap — общий план (снимок 10.09.2026)

Сводка всех фаз и фич. Детали: `f1-kitchen-plan.md` (заготовка openf1,
15 амендментов), `railway.md` (фаза C). Статусы: ✅ сделано, 🔶 в работе/
ждёт события, ⬜ не начато, 🔑 — требуется решение/действие владельца.

## Сделано к сегодняшнему дню

- ✅ Клиент **0.6.6** (v0.6.6 build 252, e6b6521): снятие фолбэков
  (IMSA-каскад+points, оверлеи календаря+loadYearMeetings, перепроводки
  raceDetails/standings), погода mirror-first (ForecastSource + now +
  бейдж возраста), читатель Б1, дизамбиг CHE. Проверен владельцем,
  закоммичен, В TESTFLIGHT НЕ ОТПРАВЛЕН.
- ✅ **Заготовка openf1** целиком (этапы 0–4): сырья и вербатима FIA в
  публичном репо нет, витрина не сдвинулась ни байтом, каскад 2023–24
  жив. Сырьё — в приватном raw-зеркале (rawmirror.yml, суточно).
- ✅ **Фаза A** прятания (sourceleak-сторож, флаги в бандл, погода без
  source) — август.
- ✅ **Фаза B**: бэк прогноза (forecast/now/coords, CI зелёный) +
  клиентская половина в 0.6.6 (прямой Open-Meteo — страховка до 0.6.7).
- ✅ **Механика фазы C** вся: оркестратор (паритет 6 воркфлоу, три
  режима), exportserve, serve-шаг (гейтован на origin, нейтральные
  «data update»), вебхук-нотификатор, Dockerfile/entrypoint, док с
  чеклистом. Отрепетировано юнитами; контейнер не гонялся (нет Docker).
- ✅ **Б1**: schedule-блок файла события (бэк) + клиентский читатель.
- ✅ **Запись live-фида Монцы** (14.7 МБ, 53/53 круга, /signalrcore) —
  лежит в привате, НЕ разобрана.

## Трек 1 — релизы клиента

1. 🔑 **0.6.6 → TestFlight** — готов, решение о моменте за владельцем.
2. ⬜ **0.6.7** (после обкатки 0.6.6 ≥1 цикла):
   - снять jolpica-ветки промаха current* и цепочки OpenF1 экрана
     события (протоколы/рейс-контрол/топ-3);
   - снять прямой Open-Meteo, словарь координат, makeTypical
     (страница живёт на витрине; sourceleak-белый список сужается);
   - **About-экран с атрибуциями** (Jolpica, OpenF1, Open-Meteo CC-BY,
     Wikipedia) — ОБЯЗАТЕЛЬСТВО лицензий, без него наружу нельзя;
   - решение по глубокому архиву погоды (WEC 2011+): бэкфилл или
     честное отсутствие страницы (нужна разведка событийных id).
   - Оговорка N+1: фолбэк loadSessions для событий без файла обязан
     пережить 0.6.7 (файл теста февраля-N+1 появляется 1 января).

## Трек 2 — данные и правовое

3. 🔶 **Обкатка Мадрида** (11–13.09): первый живой уик-энд f1live на
   фактах; после этапа — ритуалы `npm run f1pitawards` (openf1 болеет
   stop_duration) и preferredTire Мадрида.
4. ⬜ **Фаза D-лайт** (окно «до раздачи 0.6.6» — сейчас): sourceIds →
   свои поля (confirmed/round), sourceIds.fiawec выпилить,
   `f1-meeting-*` → слаги, meetingKey → нейтральное поле. После неё в
   витрине НОЛЬ текстовых имён источников. **D-финал** (свои числовые
   id) — только после 0.6.7/фазы 6 (каскад 2023–24 держит ключи).
5. 🔑 **Фаза C — сплит + Railway.** От владельца: аккаунт + Hobby, имя
   публичного serve-репо, два deploy-ключа (команды в railway.md §3),
   опц. Docker Desktop для локальной репетиции образа. Затем день Х:
   код+сырьё в приват, пересоздание публичной истории (критерий — 404
   по старым SHA; закрывает и хвост «сырьё в git-истории» из амендмента
   8), сервисы по чеклисту §2, неделя наблюдения (weclive — мерило).
   С фазой C уезжают moments.json, refs/matching.json, health.
6. 🔑 **overlap-assets** — critical правовой блокер стора (медиа):
   нужен продуктовый разговор о замене спорного.

## Трек 3 — лайв

7. ⬜ **Разбор записи Монцы** (чип-задача висит): выжимки —
   stationary-питы (сверка с DHL/openf1), стинты, полный рейс-контрол;
   вывод о форме «заготовки лайва» и решение, писать ли следующие
   гонки (рекордер готов, запуск одной командой).
8. ⬜ **Лайв-лайт** (после фазы C): квази-лайв клиенту поверх
   f1live/weclive-каденса; протокол /signalrcore разведан. Красные
   линии: радио-аудио и телеметрию наружу не отдаём (CarData в
   открытом фиде больше не существует).

## Трек 4 — фичи (конкурентный порядок, после 0.6.6)

9. ⬜ **S-полка** (на готовых данных): список FIA-документов этапа,
   полная лента Race Control, стинт-чарт, cutoff-времена квалы YoY,
   penalty-топ; каунтдаун/F1TV-ссылка/Heat Hazard.
10. ⬜ **Экран пилота** (L; закрывает аудит-долг и гэп с Box Box).
11. ⬜ **Колокольчик/пуши** — APNs-мини-сервис на Railway (после C).
12. ⬜ TG-новости — отложено; Railway-скрейпер как плюшка фазы C+.

## Трек 5 — сезонное/операционное (фоново)

- brands.json — обновлять после побед/титулов (правка JSON без релиза).
- Б2: `f1/calendar/2027.json` — когда источник опубликует (осень);
  тогда же снять живую jolpica-ветку seasonSchedule.
- Season rollover 2027 (аудит июля): чеклист карт/ассетов — к зиме.
- Бампы версий экстракции openf1 — только ручной 4-шаговой процедурой
  (задокументирована у констант openf1facts.ts).

## Рекомендуемый порядок ближайших шагов

1) D-лайт (окно закрывается раздачей 0.6.6) → 2) 0.6.6 в TestFlight →
3) разбор записи Монцы (параллельно с обкаткой Мадрида) → 4) S-полка →
5) фаза C, как только будет аккаунт (любой момент — механика готова) →
6) 0.6.7 + About → 7) экран пилота → 8) лайв-лайт и пуши.
