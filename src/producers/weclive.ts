// Продьюсер «идущий этап WEC» — тонкая обёртка над lib/weclive.ts (там же
// причина существования и экономика). Запускается своим воркфлоу weclive.yml
// каждые ~15 минут; если живого этапа нет, выходит нулём, не тронув сеть.
//
// Ошибку fiawec НЕ считаем поводом валить прогон: этот продьюсер — ускоритель
// свежести, а не источник истины. Полный проход wec.ts идёт своим чередом и
// имеет собственный алерт-гейт; письмо владельцу каждые 15 минут из-за блипа
// сети было бы хуже, чем пропущенное обновление.

import { runWecLive } from "../lib/weclive.js";

async function main() {
  console.log(await runWecLive(Date.now()));
}

if (import.meta.url === `file://${process.argv[1]}`) {
  main().catch((e) => {
    console.warn(`wec live: прогон не удался — ${e}`);
    // exit 0 намеренно: см. шапку.
  });
}
